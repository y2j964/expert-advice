<li class='question-list__item'>
  <LinkTo @route="question" @model={{@slug}}>{{@title}}</LinkTo>
  <div class="tags">
    <LinkTo @route='edit' @model={{@slug}} class='btn btn-sm'>edit</LinkTo>
    <button class='btn btn-sm' onclick={{action "deleteQuestion" @id}}>delete</button>
  </div>
</li>
